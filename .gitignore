# ------------------------------------------------------------------------------
# 1) Python cache and compiled files
# ------------------------------------------------------------------------------
__pycache__/
*.py[cod]

# ------------------------------------------------------------------------------
# 2) Virtual environments and system directories
# ------------------------------------------------------------------------------
venv/
.venv/
virflux-env/
bin/
lib/
lib64/
share/
pyvenv.cfg

# ------------------------------------------------------------------------------
# 3) Environment files (keep .env.example, ignore real secrets)
# ------------------------------------------------------------------------------
.env
*.env

# ------------------------------------------------------------------------------
# 4) Output artifacts (optional: keep if you want to version outputs)
# ------------------------------------------------------------------------------
# Ignore everything inside 'outputs' but keep the folder itself
outputs/*
!outputs/.gitignore

# Ignore everything inside 'outputs/tts' but keep the folder itself
outputs/tts/*
!outputs/tts/.gitignore

# Ignore everything inside 'models' but keep the folder itself
models/*
!models/.gitignore
